export interface Options {
  // The image path you need to find
  imagePath?: string[]
  // The image type
  imageType?: string[]
  // If the picture is duplicated, will it exit the process?
  exitOnDuplicate?: boolean
}
